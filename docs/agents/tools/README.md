# Agent Tools

Store reusable helper scripts created for agent workflow support here.

If a script becomes part of the product or maintenance workflow, promote it into the repository's normal `scripts/` or `frontend/scripts/` location instead.
